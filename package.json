{
  "name": "focusflow-workspace",
  "private": true,
  "scripts": {
    "dev:admin": "pnpm --filter admin_webapp dev",
    "dev:desktop": "pnpm --filter desktop_app dev",
    "dev:backend": "pnpm --filter backend dev",
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  }
}